apiVersion: v1
kind: Service 
metadata:
  name: typing-server
  namespace: dev
  labels:
    tier: backend 

spec: 
  selector:
    app: backend
  type: NodePort
  ports:
    - name: "backend-port"
      port: 3000
      targetPort: 3000
      protocol: TCP
